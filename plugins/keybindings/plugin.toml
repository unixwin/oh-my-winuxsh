name = "keybindings"
version = "1.0.1"
kind = "bridge"
entry = "keybindings.plugin.winux"
summary = "Bridge for official Reedline keybinding preset assets."
permissions = []
required_binaries = []

[exports]
keybindings = ["common", "emacs", "vi"]
host_bridge = "reedline-keybindings"
