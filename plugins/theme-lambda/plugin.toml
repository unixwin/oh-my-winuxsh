name = "theme-lambda"
version = "1.0.1"
kind = "source"
entry = "theme-lambda.plugin.winux"
summary = "Lambda-style minimal Oh My Zsh prompt theme plugin."
permissions = ["shell:source"]
required_binaries = []

[exports]
themes = ["lambda"]
