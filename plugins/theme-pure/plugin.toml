name = "theme-pure"
version = "1.0.1"
kind = "source"
entry = "theme-pure.plugin.winux"
summary = "Pure-inspired two-line official prompt theme plugin."
permissions = ["shell:source"]
required_binaries = []

[exports]
themes = ["pure"]
