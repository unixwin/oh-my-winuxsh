name = "theme-classic"
version = "1.0.1"
kind = "source"
entry = "theme-classic.plugin.winux"
summary = "Classic two-line official prompt theme plugin."
permissions = ["shell:source"]
required_binaries = []

[exports]
themes = ["classic"]
