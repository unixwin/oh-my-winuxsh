# command-not-found bridge plugin.
#
# The current provider is still implemented by the Winuxsh host. This plugin is
# the directory-first ownership marker so the feature can migrate behind the
# plugin API instead of remaining an invisible builtin.

WINUXSH_COMMAND_NOT_FOUND_PROVIDER=command-not-found
export WINUXSH_COMMAND_NOT_FOUND_PROVIDER
